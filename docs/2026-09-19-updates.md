# 2026-09-19 Updates — Accurate Project Status

## Verified Build State

- `cargo check --release` compiles with 0 warnings, 0 errors.
- `cargo test --lib` passes: 36 tests, 0 failures.
- No compilation errors.

## Actual Implementation State

### Fully Implemented Modules
- `core.rs` (~700 lines): constants, `FilterType` enum (12 types), `BiquadCoefficients`, `EqBand` with `solo`, `band_biquad_coefficients()` for 9 selectable filter types, `default_bands()`, `format_frequency()`, `log_response_frequencies()`, `stepped_response_frequencies()`, `biquad_response_at_frequency()`, `total_response_db()`, `total_response_db_at_frequencies()`, `estimate_response_peak_db()`, `identity_biquad_coefficients()`, `eq_band_to_dict()`, `eq_band_from_dict()`, `preset_payload()`, `preset_payload_state_signature()`, `preset_payload_bands()`, `save_preset_to_file()`, `load_preset_from_file()`, `delete_preset_file()`, `preset_storage_dir()`, `preset_path_for_name()`, `ensure_preset_storage_dir()`, `output_preset_links_path()`, `load_output_preset_config()`, `write_output_preset_config()`, `get_output_preset_fallback_name()`, `set_output_preset_fallback_name()`, `clear_output_preset_fallback_name()`, `get_output_preset_link_match()`, `set_output_preset_link()`, `clear_output_preset_link()`, `FILTER_TYPE_INDEX_BY_VALUE`, `MODE_ORDER`, `MODE_INDEX_BY_VALUE`, 9 unit tests.
- `autoeq.rs` (704 lines): APO parser, entry search, API client, 9 tests passing.
- `ebur128.rs` (122 lines): LUFS metering wrapper, 4 tests passing.
- `settings.rs` (223 lines): JSON settings persistence, appearance/background/start-at-login, 2 tests passing.
- `analyzer.rs` (593 lines): FFT spectrum, smoothing, display gain, PCM conversion.
- `band_fader.rs` (~746 lines): Full cairo-rendered vertical fader with upstream interaction model (drag threshold, Shift/Ctrl modifiers, scroll, keyboard, M/S badges, frequency/Q labels, gradient track, tick marks).
- `window_layout.rs` (137 lines): `AdwOverlaySplitView` + `AdwClamp(max=1480)` + `AdwToastOverlay`, breakpoints at 1320sp/1080sp, F9 toggle, responsive fader heights.
- `window_utility.rs` (340 lines): Preset section + system section with headroom 3-segment meter + monitor strip.
- `window_headroom.rs` (204 lines): 3-segment meter, Set Safe button, state CSS classes.
- `window_analyzer.rs` (154 lines): Smoothing/display-gain/freeze/LUFS/summary controls.
- `window_graph.rs` (300 lines): 3-layer DrawingArea overlay, click+drag editing, selected band visuals, uses `total_response_db_at_frequencies` for accurate frequency response.
- `style.rs` (214 lines): GTK CSS loading.
- `appearance.rs` (86 lines): Light/dark/system appearance preference.
- `dbus_control.rs` (475 lines): D-Bus remote control with `AppState` handler.
- `background.rs` (303 lines): Background mode, start-at-login logic, Flatpak portal support.
- `instance.rs` (29 lines): Single-instance guard.
- `desktop_integration.rs` (230 lines): Desktop file, autostart, app icon installation.
- `main.rs` (177 lines): CLI parsing via clap, headless/background modes, GUI launch.
- `window_presets.rs` (~440 lines): Preset panel with revert/reset buttons, preset combo, current curve row, state chip, file monitoring, `load_library_preset()`, `reset_to_neutral()`, `revert_to_baseline()`.
- `window_autoeq.rs` (~288 lines): Functional AutoEq import dialog with search entry, results list, refresh button, curve preview DrawingArea that downloads and parses the selected preset to show the frequency response, async entry loading.
- `window_preferences.rs` (~85 lines): Functional `AdwPreferencesDialog` with Background group (Keep Running in Background, Start at Login, Enable System-wide EQ at Login), wired to settings persistence.

### Removed Modules
- `cli.rs`: Removed; CLI is implemented in `main.rs` via clap derive.
- `screenshot.rs`: Removed empty stub.
- `diagnostics.rs`: Removed empty stub.
- `release_notes.rs`: Removed empty stub.

### Backend Status
- `pipewire_backend.rs` (420 lines): Creates virtual sink, filter-chain, output node via `core.create_object`; configures biquad props; links nodes. **Unverified on live PipeWire** — node creation and linking are best-effort stubs.
- `routing.rs` (382 lines): `RoutingEngine` with route detection, auto-route, stream scanning. **Unverified on live PipeWire** — metadata-driven default sink following and stream relinking are incomplete.

### What Is Missing From Original
- Real PipeWire filter-chain DSP with working virtual sink/output routing on live session.
- Full preset lifecycle: fallback presets UI, output-preset route linking UI.
- Window state: monitor geometry fallback for initial size is implemented but untested on multi-monitor setups.
- GNOME Shell extension, Flatpak manifest.
- Analyzer + headroom live data feed from PipeWire monitor stream.

## Next Steps

1. Backend: verify/complete PipeWire node creation, filter-chain module loading, biquad param application, stream routing on live session.
2. Presets: wire fallback preset and output-preset linking UI.
3. GNOME Shell extension: implement extension for system-wide EQ status.
4. Runtime validation: add integration tests for preset lifecycle.
