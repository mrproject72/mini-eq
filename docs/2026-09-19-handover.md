# 2026-09-19 Handover

## Session Summary

Continued preset lifecycle implementation. Added revert/reapply, file monitoring, preset combo, state chip, and output-preset/falback core functions. Wired preset panel callbacks to band faders.

## Verified Facts

- Project path: `/home/mrproject/code/mini-eq`
- Upstream remote: `https://github.com/bhack/mini-eq.git` (fetched, tags v0.1.0–v0.8.8 available)
- `cargo check --release`: 0 warnings, 0 errors
- `cargo test --lib`: 36 tests passing, 0 failures

## Actual Implementation State

### Completed This Session
- **core.rs preset lifecycle**: Added `delete_preset_file()`, `output_preset_links_path()`, `load_output_preset_config()`, `write_output_preset_config()`, `get_output_preset_fallback_name()`, `set_output_preset_fallback_name()`, `clear_output_preset_fallback_name()`, `get_output_preset_link_match()`, `set_output_preset_link()`, `clear_output_preset_link()`, `preset_payload_bands()`.
- **window_presets.rs**: Refactored `PresetPanel` to use `Rc<RefCell<Self>>` for self-referencing closures. Added revert/reset buttons, preset combo, current curve row, state chip. Added `load_library_preset()`, `reset_to_neutral()`, `revert_to_baseline()`, `set_curve_revert_baseline()`, `update_state_chip()`. Wired file monitoring via `Gio.FileMonitor`. Fixed save button to use `current_bands` instead of `default_bands()`.
- **window.rs**: Wired `PresetPanel` callbacks (`apply_bands`, `reset`, `get_signature`) to band faders and graph update loop.
- **window_utility.rs**: Updated `UtilityPane` to store `Rc<RefCell<PresetPanel>>` and call `start_file_monitoring()`.

### Still Stub / Missing
- `pipewire_backend.rs` and `routing.rs` compile but are **unverified on live PipeWire**.
- Preset lifecycle: revert/reapply and file monitoring implemented; fallback presets and output-preset linking have core functions but no UI wiring.
- No GNOME Shell extension.
- No runtime validation tests beyond unit tests.

## Key Files Changed This Session

- `src/core.rs`: Preset lifecycle core functions
- `src/window_presets.rs`: Revert/reapply, file monitoring, preset combo, state chip
- `src/window.rs`: Preset panel callbacks wired to band faders
- `src/window_utility.rs`: Updated to use `Rc<RefCell<PresetPanel>>`

## Next Steps (Priority Order)

1. **Backend parity**: verify/complete `pipewire_backend.rs` + `routing.rs` on live PipeWire session.
2. **Presets**: wire fallback preset and output-preset linking UI.
3. **GNOME Shell extension**: implement extension for system-wide EQ status.
4. **Runtime validation**: add integration tests for preset lifecycle.
